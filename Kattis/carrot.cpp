#include <bits/stdc++.h>

using namespace std;

int main() {
        ios::sync_with_stdio(0);
        cin.tie(0);
        int n, p;
        cin >> n >> p;
        for(int i = 0; i < n; ++i) {
            string t;
            cin >> t;
        }
        cout << p << "\n";
        return 0;
}
